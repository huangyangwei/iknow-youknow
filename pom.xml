<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.0.7</version>
    <relativePath/>
  </parent>

  <groupId>com.huangyangwei</groupId>
  <artifactId>iknow-youknow</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>iknow-youknow</name>
  <description>企业知识库问答系统 iknow-youknow — 后端多模块骨架（P0，技术方案 technical-plan.md §6）</description>

  <modules>
    <module>iknow-common</module>
    <module>iknow-infra</module>
    <module>iknow-api</module>
    <module>iknow-module-auth</module>
    <module>iknow-module-knowledge</module>
    <module>iknow-module-ai</module>
    <module>iknow-server</module>
  </modules>

  <properties>
    <java.version>17</java.version>
    <mybatis-plus.version>3.5.17</mybatis-plus.version>
    <spring-ai.version>2.0.0</spring-ai.version>
    <jjwt.version>0.12.6</jjwt.version>
    <nacos-client.version>2.4.3</nacos-client.version>
    <zonky.version>2.2.2</zonky.version>
    <jsoup.version>1.17.2</jsoup.version>
    <commonmark.version>0.22.0</commonmark.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- MyBatis-Plus：Boot4 官方 starter；3.5.9+ 起分页等 JSqlParser 拦截器拆分为独立 artifact，必须显式引入（M0 验证） -->
      <dependency>
        <groupId>com.baomidou</groupId>
        <artifactId>mybatis-plus-spring-boot4-starter</artifactId>
        <version>${mybatis-plus.version}</version>
      </dependency>
      <dependency>
        <groupId>com.baomidou</groupId>
        <artifactId>mybatis-plus-jsqlparser</artifactId>
        <version>${mybatis-plus.version}</version>
      </dependency>
      <!-- Spring AI BOM（M0 锁定 2.0.0，P2 问答模块起使用） -->
      <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-bom</artifactId>
        <version>${spring-ai.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <!-- JWT（HS256） -->
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-api</artifactId>
        <version>${jjwt.version}</version>
      </dependency>
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-impl</artifactId>
        <version>${jjwt.version}</version>
      </dependency>
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-jackson</artifactId>
        <version>${jjwt.version}</version>
      </dependency>
      <!-- Nacos 配置中心客户端（技术方案 R4 兜底：SDK 拉取配置并绑定 @ConfigurationProperties） -->
      <dependency>
        <groupId>com.alibaba.nacos</groupId>
        <artifactId>nacos-client</artifactId>
        <version>${nacos-client.version}</version>
      </dependency>
      <!-- 测试用嵌入式 PostgreSQL 16 -->
      <dependency>
        <groupId>io.zonky.test</groupId>
        <artifactId>embedded-postgres</artifactId>
        <version>${zonky.version}</version>
      </dependency>
      <!-- 富文本 HTML 解析/净化（发布时 HTML→纯文本双通道） -->
      <dependency>
        <groupId>org.jsoup</groupId>
        <artifactId>jsoup</artifactId>
        <version>${jsoup.version}</version>
      </dependency>
      <!-- Markdown 批量导入（commonmark + GFM 表格扩展） -->
      <dependency>
        <groupId>org.commonmark</groupId>
        <artifactId>commonmark</artifactId>
        <version>${commonmark.version}</version>
      </dependency>
      <dependency>
        <groupId>org.commonmark</groupId>
        <artifactId>commonmark-ext-gfm-tables</artifactId>
        <version>${commonmark.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <configuration>
            <!-- 沙箱 Maven 本地仓库位于不同盘符，JDK9+ URLClassPath 检查会导致 Surefire 分叉 VM 崩溃（M0 验证）。
                 JDK21+ 默认禁止运行时动态加载 agent，Mockito 内联 mock 需要放行（JEP 451）。 -->
            <argLine>-Djdk.net.URLClassPath.disableClassPathURLCheck=true -XX:+EnableDynamicAgentLoading -Djdk.attach.allowAttachSelf=true</argLine>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
