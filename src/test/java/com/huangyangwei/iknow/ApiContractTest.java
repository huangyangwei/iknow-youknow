package com.huangyangwei.iknow;
import static org.springframework.security.test.web.servlet.request.SecurityMockMvcRequestPostProcessors.httpBasic; import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.*; import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*; import org.junit.jupiter.api.Test; import org.springframework.beans.factory.annotation.Autowired; import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc; import org.springframework.boot.test.context.SpringBootTest; import org.springframework.http.MediaType; import org.springframework.test.web.servlet.MockMvc;
@SpringBootTest @AutoConfigureMockMvc class ApiContractTest { @Autowired MockMvc mvc;
 @Test void searchRequiresLogin() throws Exception {mvc.perform(get("/api/v1/search").param("q","spring")).andExpect(status().isUnauthorized());}
 @Test void searchContract() throws Exception {mvc.perform(get("/api/v1/search").param("q","spring").with(httpBasic("user","changeit"))).andExpect(status().isOk()).andExpect(jsonPath("$.query").value("spring"));}
 @Test void sseInsufficientEvidenceIsStreamed() throws Exception {String body=mvc.perform(post("/api/v1/conversations").with(httpBasic("user","changeit")).contentType(MediaType.APPLICATION_JSON).content("{\"title\":\"test\"}")).andReturn().getResponse().getContentAsString(); String id=body.replaceAll(".*:\\"([^\\"]+)\\".*","$1"); mvc.perform(post("/api/v1/conversations/"+id+"/questions:stream").with(httpBasic("user","changeit")).contentType(MediaType.APPLICATION_JSON).content("{\"question\":\"what?\"}")).andExpect(status().isOk()).andExpect(content().string(org.hamcrest.Matchers.containsString("当前已发布知识中没有足够证据"))); }
}
